# Failure modes

One row per incident that bit: what happened, the root cause, the test that was red before the fix, the rail that keeps it fixed.

| incident | root cause | fail-pre-fix test | rail |
|---|---|---|---|
| a JSON-subtyped text value stopped matching its own identity between the bulk load and incremental maintenance | `json_array(value)` embeds a JSON-subtyped text as JSON while the Rust `identity()` that wrote the other side bound it as a quoted string, so a lookup missed the stored row and the retraction was lost; the two forms differ, `json_array(subtype)` is `[["a"]]` against `json_array(subtype\|\|'')` `["[\"a\"]"]`. `85dc492` appends `\|\|''` to text in every SQL encoder | not found: no test sends a JSON-subtyped value through the identity (searched `json_type`, `subtype`, `json_extract` in `tests/`) | `plain()` at `src/1a_relational.rs:193`, `folded()` at `src/1a_relational.rs:185` and `key_sql()` at `src/0b_relational.rs:218` append `\|\|''` for text; `tests/7_key_agreement.rs:243` round trips the identity corpus and `tests/7_key_agreement.rs:236` asserts the bulk and incremental groups render alike |
| an arrangement row stored its identity composite twice, in `__c` and in the columns that rebuild it | `__c` held a text copy of the composite so `change()` compared one string against the `__r` hash hit; `fill` wrote that copy from one encoder and the bulk group insert from another, so the copy could drift from the encoder. `2e28105` drops the column and rebuilds the composite with `identity_sql` on the hit | `tests/11_intern.rs` `hashing_the_identity_keeps_one_arrangement_row_per_composite`; `2e28105` rewrites its `__c` assertions into the rebuild form | `tests/11_intern.rs:195` requires `count(*) = count(DISTINCT composite)` and `count(*) FILTER (WHERE sqlite_ivm_hash(composite)=__r)`; `tests/12_compass.rs:68` runs the same rail over the golden view |
| a join key that holds a JSON null matched another row with a JSON null | the join guard tested `json_type(value)='null'`, but `json_each.value` is SQL NULL for a JSON null and `json_type(NULL)` returns NULL, so the guard never fired and two rows with a null component joined. `a6c987c` reads the `type` column of `json_each` | `tests/4_features.rs:117` `collations_control_group_distinct_join_and_outer_predicates` puts NULL `k` on both sides of a FULL JOIN and compares the view against the oracle after every mutation | `src/1c_materialize.rs:112` tests `type='null'` and applies it at `src/1c_materialize.rs:150`, `:159` and `:187` |
| a UNION ALL view kept only its first input | `populate` materializes a `Kind::Set("all")` node from `out_table(inputs[0])` alone (`src/1c_materialize.rs:77`) and counts a read on `inputs[0]` only (`src/1b_state.rs:174` and `:186`), so the right branch never reaches the node; the set at a time drain passes every input (`a6c987c`), the bulk path does not | not found: no test seeds the second source before CREATE, and `tests/4_features.rs:18` `feature_compositions_against_sqlite` creates on empty tables (searched `union` and `Set("all")` in `tests/`) | not found: the bulk branch has no rail; `git log -S'"all", _'` returns `67abfd6`, `20743b2` and `b321ea1` (introduction and one move), and the drain rail is `src/1e_program.rs:196` |
| maintenance ran its scratch DDL inside a source hook trigger | the drain created each scratch table when it first needed it, and SQLite rejects a schema change inside a trigger program, which aborts the statement that fired it. `a6c987c` creates every scratch table at bind time | `tests/5_transactions.rs:108` `cascades_generated_values_and_user_trigger_writes_compose` writes `audit` rows from user triggers, so the engine's hook runs inside a trigger program | `src/1d_drain.rs:13` states the rule; `prepare_scratch` runs at `src/2_vtab.rs:290` and `src/2_vtab.rs:351`, outside every trigger |
| a drain cleared the staged rows and their marks, so a later `ROLLBACK TO` restored the wrong set | the collector re-stages rows at a savepoint mark, and a mid transaction drain at the first read or at `xSync` empties the staged rows, so `ROLLBACK TO` had no set to rewind from. `bdf85ee` lands the collector with this leg red and `d750255` pins the build that re-stages (hafley-rs #85, `dc20afa5`) | `tests/5_transactions.rs:202` `recursive_member_tables_follow_savepoints_and_rollback`, named red in the `bdf85ee` message | `tests/5_transactions.rs:220` rolls back to `inner_point` and re-verifies the view; `src/2_vtab.rs:512` forwards savepoint, release and rollback to the collector |
| a JSON-subtyped expression key split one group between bulk load and incremental maintenance | bulk evaluation retained the JSON subtype while incremental row transport returned plain text, producing different encoded keys | `tests/7_key_agreement.rs` `json_subtype_group_key_agrees_between_bulk_and_incremental_paths` | text key expressions append `||''` before encoding, and non-window Group inputs pass through `project_group_input` |
| a window followed by `LIMIT` dropped a multiplicity copy and selected a later product | the recursive bag expansion emitted every candidate seed before later copies, so `LIMIT` observed breadth-first CTE order | `tests/8_group_limit.rs` `window_with_limit_reads_every_copy` | the test checks three window shapes under all eight multiplicity shapes; copy ordinals expand each candidate contiguously |
| `8_group_limit` 3.7s, 73% of group_limit statements freshly prepared | `db.prepare`/`execute`/`execute_batch(format!)` in the drain path, one prepare per view per drain (`src/1a_relational.rs` drain, materialize) | `scripts/statement-costs.sh 8_group_limit`, `run=1` on 265545 events | every drain-path statement goes through `prepare_cached`; `plans/costs/README.md` ledger |
| a recursive view emitted 20 statements per source row | `Kind::Fixpoint` drained one delta row at a time through `change()` and a row-bound derive | `tests/13_statements_per_drain.rs` `reach_view: 189 at 8 rows, 2589 at 128` | the same test demands equal statement counts at 8 and 128 rows past the seed node |
| set-at-a-time fixpoint lost `(4,1)` after `UPDATE edges SET b=1 WHERE a=5` | `rowid>lo` marked new members, but SQLite reuses rowids once the newest rows are deleted, which the delete pass does | `tests/4_features.rs` `row1_binary_closure_with_cycles` | member and work tables carry `INTEGER PRIMARY KEY AUTOINCREMENT`, storage format 5; recursive views below it must be re-created |
| generation-gated `refresh` skipped the retry of a failed connect-time bind | generation stamped before the bind succeeded | `tests/3_relational.rs` `narrow_source_column_order_changes_are_transactional` | stamp lands after the bind in `Table::refresh` |
| `vm_step` per statement read as thousands for a three-value insert | `sqlite3_stmt_status` is cumulative per handle; `StmtRef` in the trace callback cannot reset it; one handle per view connection shares SQL text | none: measurement tool | `scripts/statement-costs.sh` diffs by `run` within (view, sql); `run=1` is a new handle |
| statement `nanos` read as 0 or 1000000 | the SQLite build's profile clock has 1ms granularity | none: measurement tool | the ms column is a sample count; time by node kind comes from span `time.busy` or a sampler |
| 18% of samples in `mach_vm_allocate`/`memset`/`vm_deallocate` | Apple's `libsqlite3.dylib` backs every ephemeral b-tree (IN-subquery, DISTINCT, GROUP BY, recursive CTE) with a purgeable page cache | `samply record` on the `8_group_limit` binary | `bundled` SQLite is the default feature; extension builds pass `--no-default-features` |
| three "improvements" reported from 1ms-quantized time columns and overlapping run spreads | statement counts and a noise column stood in for time | none | a change counts only when three runs all fall below the previous three; time comes from spans or a sampler |
| `chain` circuit 217 ms, 82% of samples in `upsert` | the arrangement UPDATE rebuilt `json_array(CASE typeof ...)` for the delta side on every compared row, three scans of the delta per arrangement row (`jsonArrayFunc` 18%, `VdbeSerialGet` 14%) | `samply record` on `4_sqlite_case` with the chain fixture | the delta is summed once per identity into `temp.__ivm_delta_<w>_<id>_<side>` with its hash indexed; `UPDATE ... FROM` joins it once |
| temp scratch table reused across views with a different width | scratch names carried only the node id; `CREATE TABLE IF NOT EXISTS` kept the first view's shape | `tests/4_features.rs` `feature_compositions_against_sqlite`: `table temp.__ivm_delta_3_0 has no column named c1` | every scratch name carries the width, like the out tables |
| most of the `reach` cell read as ephemeral-table setup | the profile came from a `debug = 0` binary, where `atos` attributes a sampled address to the nearest preceding symbol, so `sqlite3KeyInfoUnref` read as 12.9% and is really `sqlite3VdbeExec` | none: measurement tool | profile binaries keep symbols (`bench/Cargo.toml` `debug = 1`); a frame share is only quoted from a symbolized profile |
| recursive departure ran 52236 delete rounds and restore rescanned the recursive input for every work row | the delete loop advanced a rowid slice one hop at a time, then a correlated restore tested each departed row independently | `RUST_LOG=sqlite_ivm=debug` reach fanout 1 n 40000 measured 52236 delete spans; `EXPLAIN QUERY PLAN` showed `CORRELATED SCALAR SUBQUERY` and `SCAN input` | each departure rule closes in a recursive CTE, restore runs one set join per rule, and the same probe measures 159 delete spans with indexed searches of the input and work relations |
