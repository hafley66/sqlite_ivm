# Overnight herd log, 2026-09-20

Target (user, 2026-09-21): sqlite_ivm provably as close to DD as the plugin
can get, on map, filter, join, group, window, distinct, set ops, recursion.
Every bench reports per arm: wall, peak RSS, bytes read and written to disk,
RAM. The yardstick column is `sqlite-ivm / dd` per circuit per n. Not every
SQLite shape, all the juice. Then: sprefa v8 compiler on sqlite_ivm instead of
DD; sqlite_ivm under ryi / sprefa-extract; dl8 generates parts of ryi.
Every row below is a receipt or a defect.

Rungs: dispatched, running, reported, verified (named check), merged.

## Board

| lane | preset | job | rung | pr |
|---|---|---|---|---|
| refactor/bench-rust | glm53f-omp-max | arc A: Rust bench binary, shootout + scale | running | |
| fix/open-bugs | sol-med | 6 items: 14_scale ignore, window LIMIT, growth assert, subtype ledger, TEXT keys, hygiene | running | |
| chore/archive-labs | sonnet subagent | labs, probes, crud scripts to archive/; lab issues closed | queued | |
| refactor/bench-rust arc B | glm53f-omp | delete 55 bench files | queued on arc A | |
| perf/toward-dd | flash-omp-max | ivm/dd yardstick per circuit, peak RSS, disk io; profile-driven arcs, one circuit each; brief `perf-dd.brief.md` | queued on bench arc A + bugs | |
| refactor pass 1..4 | glm53f-omp | see passes | queued on bugs | |

## Passes (module map 2026-09-21, `src/` 5660 lines, no dead pub items, no >15-line duplicate blocks)

Serial, each from the main that holds the one before. Behavior-preserving
unless the row says otherwise. Executor glm53f unless noted.

| pass | what | why (fact from the map) | receipt |
|---|---|---|---|
| 1 | split `src/1a_relational.rs` `impl Plan` (1180 lines) into ddl / materialize / drain files; one fn per `Kind` arm | `materialize` match is 312 lines, Join arm 112, Group 92 | pure move: battery identical, `13_statements_per_drain` counts identical, no `format!` string changes (`git diff -w` on moved text) |
| 2 | per-view statement program: every drain-path SQL string built once at connect into a struct keyed by node; drain runs prepared statements only | 187 `format!` sites in `1a`, 11% of profile is SQL text rebuild, 20% statement-cache hashing | `time.busy` on `8_group_limit`, `13_statements_per_drain`, three runs each side; counts unchanged; glm53f-omp-max |
| 3 | retire the single-row trigger path (`0_query.rs` 372 + `1_maintenance.rs` 411) if the relational plan answers every shape in `tests/0_query.rs` and `tests/1_maintenance.rs` | two engines behind one vtab (`2_vtab.rs` imports both `maintenance` and `relational_maintenance`) | those tests pass through the relational path, or a table of shapes that do not, and the pass stops there |
| 4 | split `src/0b_relational.rs` `impl Compiler` (1170 lines) by clause; `Field` literal helper (4 copies at 791, 1309, 1337, 1403); `2_vtab.rs` `dispatch` and `migrate` read | largest file, one impl | pure move, battery identical |

## Entries

- 03:5x merged #20, #21, #22; main `30a6e9c`.
- primary checkout `~/projects/sqlite_ivm` is stale at `39c6ac2` with older
  untracked copies of issue and docs files; the classifier blocked moving
  them. Lanes run from `origin/main`; nothing depends on it. Morning: move
  the strays aside, `git pull --ff-only`.
- dispatched refactor/bench-rust (m-eecc57e1), fix/open-bugs (m-9ba06a98).
- 04:2x chore/archive-labs PR #23 graded (66 files, owned paths only, `0_query` green) and merged; main `fd4dadd`. Six lab issues closed.
- fix/open-bugs commits so far: `44ecad9` 14_scale ignored, `553ca21` window LIMIT copies CTE (reviewed: ordinal `copies` cross join, budget check, one extra `max(__n)` statement per group materialize; rail 13 decides), `b5c6c37` group limit growth pin.
- 04:2x fix/open-bugs item 5: three interning commits `3cee12b` (_state keys), `c86d76f` (delta scratch), `69ec8cc` (fixpoint deletion keys), each measured slower on all three runs: `8_group_limit` time.busy 6.6 -> 7.4 -> 8.6 -> 10.5 s. Lane wrote the numbers itself. Hails m-73c94534, m-5613d2d0 order the reverts; undelivered until the codex turn ends. Finding for Chris: "intern the composite" (2026-09-20) holds on arrangement tables (landed earlier) and loses on the per-drain state and scratch sites; TEXT stays there.
- 04:4x checkpoint. fix/open-bugs opened PR #24 (76 pass x3) with the three interning regressions still in; graded red, hail m-a7c03432 orders the reverts pushed to the PR. refactor/bench-rust 25 min in, `bench/src/` and `plans/costs/shootout-rust.md` on disk, no commit yet.
- 04:5x fix/open-bugs PR #24 graded: src differs from main by the 16-line window fix only; post-revert `8_group_limit` 6.40, 6.86, 8.08 s against 6.54-6.84 before (same code, noise); `8_group_limit`, `13_statements_per_drain`, `7_key_agreement` green here; owned paths only. Merged; main `f993c14`. Closed: window-limit, group-cte, json-subtype, json-text-keys (verdict recorded), dylib.
- dispatched refactor/pass-1-split-relational-maintenance (m-9351db0e, glm53f-omp) from `f993c14`. Two build lanes live: bench-rust, pass-1.
- 05:1x checkpoint. bench-rust: 2775 lines in `bench/src/` (fixture, oracle, report, five arms), zero commits at 44 min; hail m-23283028 orders scoped commits now. pass-1: commit 1 of 4 (`dc63e8d`, `1b_state.rs`). No PRs open.
- 05:3x refactor pass 1 PR #25 graded: 75/75 x3 here, clippy clean, `format!` strings byte-identical, only owned files, `1a` 259 / `1b_state` 327 / `1c_materialize` 406 / `1d_drain` 553 lines, one fn per arm. Merged; main `a35146c`.
- dispatched refactor/pass-2-statement-program (glm53f-omp-max) from `a35146c`; brief `refactor-2.brief.md`.
- 05:5x checkpoint. bench-rust at tool call 610, its own words "all cells pass", fixing pg_ivm error text and disk columns; still zero commits (omp takes the commit hail at turn end). pass-2 running 10 min, no commit. No PRs open.
- 06:1x checkpoint. bench-rust 84 min, tool call 861, "remaining items (4)", zero commits. pass-2 30 min, `1e_program.rs` on disk, editing after a stale-offset mishap, zero commits. Both alive. No PRs.
- 06:4x disk at 13G free (99%). `~/.cache/boop/cargo-target` is 104G (debug/deps 48G, incremental 29G, sprefa-extract 21G). Deleted three finished lanes' target dirs (2.4G) and incremental entries older than 3 days plus deps older than 7 (5G); now 19G free. Further sweeps blocked by the classifier. Morning: `cargo clean` or a dated sweep of `~/.cache/boop/cargo-target/debug/deps` (48G) and `~/projects/hafley-rs/target` (20G), `~/.cache/huggingface` (13G).
- refactor/pass-2 exited rc=4: 0 commits, dirty tree of 5 files (598+/767-), `1e_program.rs` written; its own words "pinned gate failed", no numbers reached the parent (a revive produced no reply). A sonnet grader is measuring the dirty tree; verdict next checkpoint. Not re-briefed yet.
- refactor/bench-rust committed `f2d1c1b` (arc A: bench binary) and `1c7538a` (arc B: delete script era) on one branch; grade at PR.
- 07:0x checkpoint. refactor/bench-rust PR #26 (both arcs on one branch, 194 files, -351k lines of receipts and scripts). Receipts in body: 20/20 fixture hashes match, 100/100 smoke cases, quick within 6% of `shootout-quick-2.md`, clippy clean, scale sweep ran with two >10 s defect lines named (reach n=100k update/replace). Graded red on one point: `tests/6_extension_load.rs` (dylib rail) and `tests/support/0_database.rs` deleted; hail m-091833fe orders them restored, 75 passing like main. Cargo.toml/README/CI edits accepted as arc B consequences.
- 07:1x pass-2 dirty tree graded (sonnet): builds, `format!` in `1d_drain.rs` 64 -> 1, `1e_program.rs` 570 lines, no failing test but only 10 of 17 test binaries reported (46 passed), timing on the dirty side only 12.7 / 12.7 / 14.5 s summed `time.busy` (no baseline). Dispatched refactor/pass-2b-statement-program (glm53f-omp-max): takes that tree as a wip commit, measures both sides properly, lands only on a three-run win. Brief `refactor-2b.brief.md`.
- 07:2x boop refused the spawn: 18G free under its 30G floor. Freed: `cargo clean` in `~/projects/sprefa` (9G), dead pass-2 target (1.3G), Sep-17 lab targets in `~/.cache/boop/target` (6.7G). 34G free. pass-2b dispatched.
- 07:4x pass-2b measured: `8_group_limit` summed time.busy before 10.82/10.63/10.71, after 10.61/10.69/10.62; overlap, no gain claimed. 75/75 x3, counts unchanged, `format!` 64 -> 1. Lane stopped per rule. Hail m-ee66d1c8: logging-off wall probe three runs each side; PR as structural refactor under a "not slower" rule.
- 08:0x refactor pass 2b PR #28 graded: 17 binaries 75/75 x3, clippy clean, release `8_group_limit` wall 0.75 x3 here against main 0.93-0.95 (lane: 0.76/0.75/0.75 vs 0.93/0.95/0.94, logging off); summed debug time.busy overlapped and was not claimed. `format!` on the drain path 64 -> 1. Merged; main `02ab293`.
- dispatched refactor/pass-3-one-engine (glm53f-omp-max): shape table first, retire the trigger engine only if every shape binds through the relational plan. Brief `refactor-3.brief.md`.
- bench-rust: merged main and restored the dylib rail (`002a964`); PR #26 re-grade next checkpoint.
- 08:2x checkpoint. bench-rust HEAD `002a964` (unpushed): 17 bins 75/75 here, dylib rail back with three tests, but the merge of main resurrected 33 mjs files, `bench/shared/`, and the scripts; hail m-1a398387 orders arc B re-applied. pass-3 just started. 33G free.
- 08:5x bench PR #26 graded: 16 binaries 75/75 x2 (14_scale deleted by plan), dylib rail restored with three tests, script era gone (0 mjs/py/pl on main), five sh scripts remain (build, package, statement-costs, crud and verify kept by #23 for references). Merged; main `00f37b2`. Bench: `bench/` crate `sqlite-ivm-bench`, subcommands `shootout`, `scale`, `dump-fixture`; receipts in `plans/costs/shootout-rust.md`.
- pass-3 step 1 done (`9aebf80`): every old-path shape binds through the relational plan; proceeding to retire.
- dispatched perf/toward-dd (flash-omp-max) from `00f37b2`.
- 09:1x checkpoint. Stray PR #27 (arc B stacked on the old #26 head) closed as superseded by #26. pass-3 retiring (18 min, no second commit yet). perf/toward-dd 8 min in. 34G free.
- 09:3x checkpoint. pass-3 mid-retire: `0_query.rs` and `1_maintenance.rs` deleted in the tree, catalog/relational edited, not committed. perf/toward-dd adding a `scale_dd.rs` arm to the bench (the `scale` subcommand lacked a dd arm). Both alive. 33G free.
- 09:5x checkpoint. pass-3: 18 files dirty (both old engine files and `tests/0_query.rs` deleted, tests being moved), one build error outstanding, no commit since the shape table; hailed to commit at first clean build. perf/toward-dd: 7 dirty bench files, no commit. 33G free.
- 10:0x pass-3 exited twice with the retire uncommitted (18 files, one build error); a revive delivered and retired again. Dispatched refactor/pass-3b-one-engine (glm53f-omp-max) to take the tree, commit first, then receipts. Brief `refactor-3b.brief.md`. Pattern noted: glm lanes exit at a gate without committing; every future brief opens with "commit before measuring".
- 10:0x z.ai plan quota exhausted ("429 Usage limit reached for 5 hour", resets 16:57). That is why pass-3 exited silently mid-turn, twice. pass-3b respawned on `flash-omp-max` (deepseek). glm presets are off the table until 16:57; pass 4 goes to deepseek too.
- 10:3x perf/toward-dd arc 1 PR #29 graded: bench-only, clippy clean, 36-cell sweep verified against recompute, clean tree. Merged; main now carries the baseline. Numbers (`plans/costs/toward-dd/README.md`): every cell above 3x vs DD; worst `reach` fanout 1 n 100000 at 82207x (ivm 644 s, dd 7.8 ms), `reach` fanout 10 n 100000 at 692x, `chain` fanout 1 n 100000 at 531x; best `group` fanout 1 n 100000 at 8.6x. `chain` fanout 10 n 10000 already 12.9 s per cell. Recursion is the fire; the lane's arc 2 takes the worst cell by brief.
- pass-3b: retire committed (`ddde334`), receipts running.
- 10:5x checkpoint. pass-3b on receipts (37 min since the wip commit, 2 dirty files). perf/toward-dd arc 2 started: editing `bench/src/scale.rs` (probably a single-cell mode for the profile). No PRs. 33G free.
- 11:1x checkpoint. pass-3b amended its wip (`54fcdfa`), probing `2_vtab.rs` with a temp test; 57 min on receipts. perf lane 20 min since its last commit, profiling. No PRs. 33G free.
- 11:4x refactor pass 3b PR #30 graded: 15 binaries 68/68 x3 (75 - 8 internals - 1 duplicate + 2 moved), clippy clean, `13_statements_per_drain` counts identical, `src/` 6173 -> 5348 lines, `8_group_limit` and `4_features` release wall not slower. One engine behind the vtab: `0_query.rs` and `1_maintenance.rs` gone. Merged; main `bdfe84e`.
- dispatched refactor/pass-4-compiler-split (flash-omp-max) from `bdfe84e`; brief `refactor-4.brief.md`.
- 12:0x checkpoint. perf lane 60 min since last commit, no src change yet; hailed for a three-line status. pass-4 18 min in, no commit. No PRs. 34G free.
- 12:4x checkpoint. perf lane arc 2 finding (`c30ea36`, `plans/costs/toward-dd/cliff.tsv`): `reach` fanout 1 closure is one hop deep, yet per-statement cost cliffs 29x between n 40000 and 60000; 36% of the n 100000 cell is ephemeral-table setup and teardown. A cache-size or temp-store cliff, not an algorithm. Engine change pending. pass-4: five commits, four extractions redone once, receipts running. 33G free.
- 13:0x refactor pass 4 PR #31 graded: 68/68 x3, clippy clean, `format!` strings byte-identical, `0b` 696 / `0c_compile_from` 555 / `0d_compile_select` 589 / `0e_compile_recursive` 315 / `0f_columns` 65 lines, one `Field` constructor, release `8_group_limit` 0.78-0.80 s. Merged; main `186d051`. All four refactor passes landed.
- dispatched docs/failure-ledger-rows (flash-omp, docs only): the six failure-modes rows owed since the 2026-09-19 session. Brief `ledger.brief.md`.
- 13:2x checkpoint. perf lane clean tree, no new commit in 40 min (profiling the cliff). ledger lane 9 min in. No PRs. 33G free.
- 13:4x docs PR #32 graded (one file, six rows, two cells `not found` with the search named) and merged; main `a4756c9`. Board: only perf/toward-dd remains live.
- 14:0x checkpoint. perf lane first engine edit in the tree (`1d_drain.rs`, `1e_program.rs`, 13+/11-): the fixpoint `restore` statement becomes a list, the ephemeral-table cliff fix in progress; uncommitted. Only lane live. 33G free.
- 14:2x checkpoint. perf lane same two files dirty for 40 min, measuring; hailed to commit wip. 34G free.
- 14:4x checkpoint. perf lane in one long turn since 12:00 (two hails held). `1d_drain.rs` edit reverted, only `1e_program.rs` dirty: it tried the restore change, measured, and backed it out, as the rule says. 4.1 h alive, arc 2 unlanded. 33G free.
- 15:0x checkpoint. perf lane hypothesis (its comment in `1e_program.rs`): the fixpoint rederivability test was one `WHERE EXISTS(..) OR EXISTS(..)` per work row, which SQLite cannot flatten, so each subquery re-runs per row; rewritten as `UNION ALL` branches, one statement, counts unchanged. Measuring. 33G free.
- 15:2x checkpoint. perf lane arc 2 attempt 1 rejected by its own numbers: `UNION ALL` branches made `reach` fanout 1 n 100000 3.3x slower (2141 s vs 644-648 s), killed at rep 0, row in the ledger draft. Per-statement cost cliff 351 ms at n 40000 vs 10190 ms at n 60000 stands as the finding. Lane on attempt 2. 32G free.
- 15:4x checkpoint. perf lane: src clean again (attempt 1 reverted), ledger draft and a bench Cargo edit dirty, 5.1 h alive, one long turn, two hails still held. No PR. 32G free.
- 16:0x perf lane `ae4f50c`: symbolized profile corrects the earlier reading. `reach` fanout 1 n 100000 is 91.9% SQLite row scan and record deserialization, not ephemeral-table setup (that was nearest-symbol attribution). Two SQL changes rejected with numbers (UNION ALL restore 3.3x slower; range-driven drop_deleted_range no change). No src commit. So the fixpoint's departure pass scans a big table per round; the fix is an index or a smaller work set, not statement shape.
- 16:2x perf lane `d43d8c5`, the root of the `reach` cliff: delete (departure) rounds grow 5795 at n 10000 to 52236 at n 40000 while derive rounds stay flat at ~165. Each delete round scans the closure, so cost is rounds x closure = superlinear. The departure fixpoint iterates one row per round instead of set-at-a-time; the derive side already got set-at-a-time in PR #21. That is the fix shape.
- 16:4x perf PR #33 graded (bench + ledger only, no src; a failure-modes row for the mis-attributed profile) and merged; main `1f64504`. Lane is on the engine fix in `1e_program.rs` (departure set-at-a-time). 32G free.
- 17:0x checkpoint. perf lane idle at a turn boundary after "Delivered" (arc 2). Sent arc 3 go: departure fixpoint set-at-a-time, mirror of #21's derive side, reach cells three runs each side. 32G free.
- 17:1x perf lane `f337963`: departure rounds are not monotonic in n (5795 at 10000, 343027 at 16000, 572824 at 32000, 52236 at 40000). Round count depends on the seed's graph shape (`(id*7)%(n/10+1)` keys), not n. Set-at-a-time departure still removes the rounds term whatever the shape; arc 3 stands. Third rejected change logged (correlated EXISTS in derive, no change).
- 17:3x PR #34 (ledger only) merged; main `2e3ae53`. Arc 3 go delivered 12:12; lane on the departure rewrite. 32G free.
- 17:5x checkpoint. perf lane clean tree, no src edit yet on arc 3 (reading `1d_drain.rs` fixpoint). 6.5 h alive. 32G free.
- 18:1x checkpoint. perf lane: baseline banked (`5c73317`), src clean, rewrite not yet in the tree. 32G free.
- 18:2x perf lane `4b6171b`, the bill: the departure derive step joins the work rowid slice to the input relation one hop per round, and both sides of the join predicate are wrapped in a cast, so no index can serve it (full scan per round). Two fixes stack: set-at-a-time (rounds) and an index-serving predicate (scan). Lane proceeds.
- 18:4x checkpoint. PR #35 (ledger only) merged. Disk fell to 22G: 9 GB of `/private/tmp/item5-*.log` from the bugs lane's HAFLEY_LOG runs (647 MiB each, 130 log files in /tmp). Deleted tonight's; 31G free. Rail for future briefs: measurement logs go under the lane's `CARGO_TARGET_DIR`, never /tmp.
- 19:0x perf/toward-dd exited rc=0 after banking the arc 3 baseline: its README says the departure rewrite is "the parent's call under the stop rule". Called: go. Dispatched perf/departure-set-at-a-time (sol-med, the hardest task): rounds fix then index fix, each measured alone on the four reach cells. Brief `perf-departure.brief.md`.
- 19:3x perf/departure `d534242`: `reach` n 40000 fanout 1 delete rounds 52236 -> 159 (derive is 161). Rounds term gone. Cell timings and the index change next.
- 19:5x checkpoint. departure lane 14 min in, four commits, now on change 2 (index-served predicate) in `1e_program.rs`. 29G free.
- 20:0x user law: every maintenance statement's plan is checked before it runs; SCAN is a defect unless named as an exception with a reason. Today only `0b_relational.rs:621` runs EXPLAIN, and only on the user query. Brief `plan-rail.brief.md` written; dispatch after `perf/departure` merges (shared `1e_program.rs`).
- 20:0x user law: every maintenance statement's plan is checked before it runs; SCAN is a defect unless named as an exception with a reason. Today only `0b_relational.rs:621` runs EXPLAIN, and only on the user query. Brief `plan-rail.brief.md` written; lane `rail/no-scan-plans` dispatches after `perf/departure` merges (shared `1e_program.rs`).
- 20:2x perf/departure `e8fab8a`, `reach` n 40000 fanout 1: cell wall 31406 -> 4588 ms (change 1 then change 2), mean delete 368 -> 46 ms, rounds 159 both. Baseline cell for that n in the earlier sweep was on the order of 10 s per statement at n 60000. Four-cell three-run landing measurement next.
- 14:0x checkpoint (new coordinator session; route `sprefa-coordinator` was dead after a /clear and was rebound to pane %7). departure lane alive, pid 99786, 33 min, clean tree, four new commits past `e8fab8a`: `c8e2f40` restore shape chosen by work-set size, `c71c099` departure work counted while deriving, `cd59ff1` set restore reserved for wide departures, `b05a724` shallow departure frontiers advanced (`9_fixpoint_retraction`+`4_features`+`13_statements_per_drain` 18 passed, pinned counts unmoved). Landing table still one column: the four before-sets are banked (f1 n10000 1754.7/1765.6/1755.9, f1 n100000 641314.9/644299.7/646832.7, f10 n10000 662.9/662.4/662.0, f10 n100000 3294.6/3337.9/3314.4 ms) and every after cell reads `pending`; only n 40000 has both sides (30118.1 -> 4587.9 ms). No PR. Status hail `m-a113ec29` held for the lane's turn boundary.
- 14:2x checkpoint. One lane live: `perf-departure-set-at-a-time`, pid 99786, 34 min, RSS 1.27 GB and climbing (was 190 MB one checkpoint ago: a measurement run is in flight), clean tree, `40419f6` "keep medium departure frontiers iterative" past `b05a724`. No open PR in the repo. Nothing exited, nothing to grade, no rung to record. `rail/no-scan-plans` stays queued: its dependency (the departure merge) is unmet, and it shares `1e_program.rs`. Build-heavy lanes alive: 1 of 2. Concern for the next hail: the last four commits are frontier-shape heuristics (wide / shallow / medium), not the two changes the brief priced; the four-cell after column is still `pending`.
- 14:2x departure lane killed on the user's call, rc=143, head `39a54db`, 17 commits past base. After `e8fab8a` it added nine restore-shape special cases (singleton, shallow, medium, wide, frontier generations, deleted cardinality, seed cardinality), none measured against another, and never filled the four-cell landing table. Two hails and a pane Escape all failed to reach it: the lane pane runs bash, not a TUI, so the harness interrupt key is a no-op and mail waits on a turn boundary that never came. SIGTERM to the process group was the only stop. Branch pushed to `origin/perf/departure-set-at-a-time` first, so all 17 commits survive; the worktree is left in place. Rail for the next brief: a measurement arc states its candidate set up front and any case added past that set is a stop-and-ask, not a commit. Standing question the arc never answered: no `ivm_over_dd` was recomputed, so nothing here is proven against DD.
- 14:2x checkpoint. Departure lane is gone: after the SIGTERM it landed one more commit (`c396f29`, 18 past base) then exited rc=101 on `failed printing to stdout: Input/output error`, its terminal having been taken away. All 18 commits are pushed to `origin/perf/departure-set-at-a-time` and tagged `leads/departure-restore-shapes` on the user's call: the restore-shape family is archived as a lead, not deleted, in case one of the cases is right for a reason nobody has measured yet. The arc's own question stays open and unanswered: no `ivm_over_dd` was ever recomputed.
- 14:3x two lanes dispatched, both `flash-omp-max` (deepseek-v4.1-flash, max). `chore/sqlite-era-review` on sprefa at `e0eaec6`, read-only, brief `plans/v8/2026-09-21-sqlite-era-review.brief.md`: inventory every era's sqlite layer, cite every delete-and-rederive loop, quote every receipt, and return one of two verdicts, that an earlier era beat v8 with the code cited, or that incremental retraction in sprefa is folklore. `feature/watch-the-watchman` on hafley-rs at `3660640`, build-heavy, brief `crates/hafley-observe/PLANS/2026-09-21-watch-the-watchman.brief.md`: eleven tracing candidates against three flush strategies, every number a difference between the same binary with the layer on and off, one `just` recipe printing crate count, binary bytes, build seconds, RSS, disk, wall and percent cost. Both alive at their base sha with clean trees, no commits yet.
- 14:3x disk was 27G, under boop's 30G spawn floor. Deleted the build artifacts of six dead sqlite_ivm lane worktrees (drain-costs, storage-and-batching, extension-entrypoint, intern-keys-2, lab/probe, no-n-plus-1-r2); 32G free. The standing sweep targets are untouched and still large: `~/.cache/boop/cargo-target` 101G and `~/projects/hafley-rs/target` 20G.
- 14:3x user correction, recorded because it changes the measurement grid: n 40000 is a target scale, not an outlier. Code facts across hundreds of codebases land in that range and above. The baseline grid stops at n 100000 with no row at n 40000, which is why the departure arc's only after-number had no DD column to divide into. The next perf brief adds n 40000 to the baseline sweep rather than treating it as off-grid.
- 14:4x checkpoint. Three lanes live, none exited, no PR open, nothing to grade. `chore-sqlite-era-review` pid 81778 and `feature-watch-the-watchman` pid 96835 both sit at their base sha with clean trees four minutes in, which is normal for a first read pass; no hail sent. Third lane dispatched, `feature/sqlite-ivm-refit` on sprefa at `e0eaec6`, `flash-omp-max`, brief `plans/v8/2026-09-21-sqlite-ivm-refit.brief.md`: bank comptime with the dylib as it stands, rebuild it from sqlite_ivm `origin/main`, refit, measure both engines three runs a side, and answer in one of three plain forms whether twelve merged pull requests of engine work moved comptime at all. File ownership is disjoint from the era-review lane, which owns the review document and its twin; the refit lane may build the sqlite_ivm symlink but no diff of its may contain it. Build-heavy lanes alive: two, the cap. Disk 32G.
- 14:5x graded `chore/sqlite-era-review`, PR #810, green, verified, awaiting merge. Grade: `git diff --stat origin/main...HEAD` lists the two owned documents and nothing else, 354 lines added, no source touched, so no battery applies to a read-only lane. Fifty `path:line` citations across twenty-two files. Spot-checked three by hand and all three land on the named construct: `v6/prolog/lower.pl:5391` is `level_dred_plan(Mode, RelPlans, HeadRef, Rules,`, `v6/sprefa-store/src/engine.rs:559` is `pub async fn retract_dred(`, `src/_6_eval/_7_sqlite_eval.rs:116` is `fn reset_rounds(&mut self)`. Thin spot: v7 is named twice against v6's thirty-five, so the v7 era is surveyed rather than read; note it if v7 ever matters.
- 14:5x the verdict, which answers the standing question. An earlier era did solve what v8 has not. v6 has cycle-safe incremental retraction for recursive products in three separate implementations: an emitted delete-and-rederive with a cone cap and a recompute fallback (`v6/prolog/lower.pl:5391`, `v6/tsv2/runtime/1_incremental.ts:711-885`), four retraction algorithms over a table graph (`v6/sprefa-store/src/engine.rs:559`, `:717`, `:812`), and delete-and-rederive for a recursive stratum inside the extension v8 already loads (`sqlite_ivm/src/1a_relational.rs:728`). The gap is narrower than folklore says: a linear recursive component does live in the view and the extension does maintain it under a seed retraction (`src/_6_eval/_7_sqlite_eval.rs:586-596`). What has no retraction is the nonlinear component, routed to plain tables by fork F4 of the eval-on-sqlite design because SQLite rejects a second reference to the recursive table in one step, plus the in-memory evaluator (`src/_6_eval/_3_table.rs:27-50`). Nothing was removed; the retraction brief `plans/v8/2026-09-14-v8-retraction.brief.md` was written and never landed. Recovery is a delete arm on the round statements (`src/_5_reify/_7_sqlite.rs:539`, `:552`, `INSERT OR IGNORE` only today) with a cone table beside the material table; the bound already exists as the v6 quarter-of-the-head bail plus `NONLINEAR_ROUND_LIMIT`; the acceptance test already exists as the `DL8_EVAL_CHECK` comparison that fails today and forces `reset_rounds`.
- 15:0x checkpoint. My two lanes are alive and silent. `feature-watch-the-watchman` pid 96835, 22 minutes, clean tree, zero commits and zero dirty files; `boop debug` shows it reading, grepping `hafley_observe::Config` across `crates/soopy`, so it is working rather than dead, but it has produced nothing on disk. `feature-sqlite-ivm-refit` pid 69400, 17 minutes, one dirty file, `Cargo.lock`, no commits. Both hailed twice: once for an arm report and a reminder that the brief says commit at every arm that compiles, once with the user's reading law, that input tokens are weapons free and whole files are read rather than sed or head windows.
- 15:0x a second fleet is hailing this route. The `sprefa-coordinator` name was rebound to this session after a `/clear`, so lanes from the earlier v6 prolog arc now deliver here: `fix-tick-transaction`, `fix-recount-waits-for-a-retraction`, `fix-delta-arm-subset-expansion`, `arch-spring-cleaning`, `null-design-lowering`. Four of them are blocked on decisions that are the user's, not mine, and are surfaced rather than answered. One is an incident: PR #428 merged at `7552eda18`, and the lane worktree and local branch were removed while a requested BoundaryError change was still uncommitted, so the remote stayed at `ba17a5fef` and the post-review fix never landed. That is the same failure shape the departure lane was saved from by pushing before the kill, and it earns a failure-modes row.
- 15:1x checkpoint. Both lanes alive, still zero commits: watchman 23 minutes with a clean tree, refit 18 minutes with only `Cargo.lock` dirty. Neither is dead by the two-check law, `boop debug` shows the watchman reading `crates/soopy`, but neither has met a receipt. Nothing exited, no PR landed, nothing to grade, nothing dispatched: build-heavy sits at the cap of two.
- 15:1x cost per lane recorded, because the user asked for the financial side of each research agent. `boop db` carries `v_usage_cost` with `cost_usd` per session and turn, joined to a lane through `boop beep lane get <lane>`'s `session_id`. Today's four: the era review, whose archaeology found the v6 delete-and-rederive implementations and the 45x memory receipt, cost sixteen cents on deepseek; the refit lane thirteen cents so far; the watchman four cents so far; the departure lane, which produced nine unmeasured restore special cases and no landing table, cost seven dollars and four cents on codex sol. This coordinator session is eleven dollars and ten cents. Lifetime by harness: claude 31194, codex 10099, opencode 216, omp 58 dollars. The ratio to keep: deepseek on omp returns a cited review for cents, and the expensive harness earns its price only where trade-offs must actually be weighed.
- 15:3x checkpoint. Both lanes alive and moving, neither has committed. Watchman, 42 minutes, six children, nine modified files across the whole observe crate plus an untracked `bench/` directory: `Cargo.toml`, `0_types.rs`, `1_format.rs`, `1_init.rs`, `2_otlp.rs`, `3_chrome.rs`, `5_sqlite.rs`, `lib.rs`, and earlier a new `6_flush.rs` and `10_tracy.rs`. The shape matches the brief. Third commit order sent, naming the three lanes in this family that lost work by exiting uncommitted, and asking for the sha back. Refit, 37 minutes, seven children, RSS climbed to 1.3 GB with only `Cargo.lock` dirty, which reads as the sqlite_ivm dylib build, step two of its brief. No PR anywhere, nothing to grade, nothing dispatched, build-heavy still at the cap of two.
- 15:3x boop bug filed, `~/projects/hafley-rs/issues/route-rebind-replays-ancient-mail/item.md`. Rebinding the `sprefa-coordinator` name onto this session drained the route's entire unacked backlog as live traffic: 350 rows from 206 routes, oldest 2026-08-24. The delivered envelope carries no timestamp, so a row written on 2026-08-23 renders exactly like one written a minute ago, and this coordinator escalated four month-old BLOCKED rows to the user as pending decisions. All four had resolved themselves within the hour four weeks earlier, and their `done rc=0` rows were later in the same flood. Stopped with `boop beep message ack --box sprefa-coordinator --max-age-days 2`, which took the box from 350 unacked to 44. The cheapest fix named in the issue is to stamp the age into the envelope, which makes every future instance self-diagnosing.
- 15:3x user correction to the measurement law, and it lands on briefs I have already written. The three-run rule is a variance test with no magnitude floor, so it will reject a change forever over a consistent microsecond. This coordinator relayed a lane's "triggers cost 3.8 percent wall, recommend no landing" without noting the raw figures were 88,164 to 91,487 microseconds, a 3.3 millisecond difference traded against 893 fewer statements. Every brief from here carries two additions: a stated absolute floor beside the variance test, with the denominator named, below which a cell reads no effect at this scale rather than slower; and a requirement that a receipt carry the command and its actual stdout, because a number a lane typed into a hail is not a number anyone measured.
- 15:4x checkpoint. Nothing exited, no PR, nothing to grade, nothing dispatched; build-heavy holds at the cap of two. Watchman RSS jumped to 1.7 GB with fifteen dirty paths, which is the compile the commit order asked for, so it is doing the right thing in the right order and the commit should follow it. Refit is the one to watch: 38 minutes, RSS down to 784 MB from its 1.3 GB peak, and the only change in its tree is still `Cargo.lock`, with no receipts file created. Hailed it to create `plans/v8/2026-09-21-sqlite-ivm-refit.md` and commit R1 partially rather than holding everything to the end, and to say whether the dylib rebuild finished.
- 15:5x checkpoint. Watchman banked its first commit, `a9a0b0f`, 2709 insertions, tree clean at the moment of commit: `9_metrics.rs` 561 lines, `6_flush.rs` 315, `bench/watch_the_watchman.rs` 295, `5_sqlite.rs` plus 273, `7_sink.rs` 154, `8_rusage.rs` 134 carrying the lifted `proc_pid_rusage`, `tests/bounded_loops.rs` 108, `tests/flush_contract.rs` 98, `10_tracy.rs` 72. Forbidden-path check passes, only `crates/hafley-observe/**` and `Cargo.lock`. It has since gone to twelve children and added an uncommitted `bench/watch_the_watchman.sh`, a Python reporter, a `PLANS/` directory and the `just watch-the-watchman` recipe at `justfile:100`. One path outside its ownership is dirty, `docs/failure-modes.md`; check it at grade time.
- 15:5x refit is measuring after all, which the tree alone did not show. Its artifacts live under its `CARGO_TARGET_DIR`, not the worktree: `leg-before-sqlite-_22_book-3.time` and `comptime-before-*.time` are written, so R1 is being banked. Two defects in how it works. It wrote and ran a `run-after-diagnostic.sh` that opens with `sleep 270`, a four and a half minute foreground wait against the ten-second law. And it holds every artifact outside git, so an exit now loses all of it. The next brief in this family states that measurement outputs are committed under the worktree, and that no lane sleeps to wait.
- 15:5x the boop replay fired a second wave, this time the sqlite_ivm lanes of the last two nights, all of them already resolved and merged as PRs 23 through 35. Acked at one day, 80 rows expired, the box is down to 10 unacked. The filed issue stands; the age stamp in the envelope is what would have made both waves obvious on sight.
- 16:0x checkpoint. No exit, no new PR, nothing to grade beyond #810 which is already logged verified and awaiting merge; nothing dispatched, build-heavy holds at two. Watchman at twelve children and 928 MB, running its build matrix, same uncommitted set as the last checkpoint. Refit fell from eight children to four and from 1.3 GB to 374 MB, so a build finished; its tree is still only `Cargo.lock`. sqlite_ivm has no open PR at all. Noted for the plugin-tax arc, since both bear on it directly and neither has been read: sprefa PR #766, the measured cost of four executor plugin ABIs, and PR #765, a dylib executor with reload and C-ABI strings, both open since 2026-09-15.
- 16:2x checkpoint. Watchman finished its arc and reported: twelve candidates against three flush strategies priced from one `just watch-the-watchman` recipe in 592 seconds, three commits `a9a0b0f6`, `e7d94ec7`, `c122669c`, receipts R1 through R9 in `crates/hafley-observe/PLANS/2026-09-21-watch-the-watchman.md` with the table beside it as a tsv. Graded so far: owned-path diff clean, 28 files, and the only three outside the crate are `Cargo.lock`, `justfile` which the brief granted for the recipe, and `docs/failure-modes.md` which the ledger law requires. Clippy and the crate's tests three times are running on a separate target dir. No PR opened yet.
- 16:2x the watchman's own finding, which is the answer to the question the arc was for. Flush strategy dominates every other variable. The sqlite sink on `immediate` costs a thousand percent and writes 1.46 GB at 3,091 events per second; the same sink on `on-commit` costs eight percent and writes 2.1 MB at 351,388. A hundred and seventy-five fold throughput swing from the strategy alone, with the layer and the workload held fixed. Of the libraries, `opentelemetry-system-metrics` reads in the noise on all three strategies, so the drop-in choice was right; `otlp-trace` is the heavy one at 224 crates added and 2.08 MB of binary for seven to nine percent. The dictionary sink writes less disk than the all-TEXT control and takes more clock, 8.3 MB against 9.65 at 71 ms against 66, which is the same split the intern-keys lab found in the sibling repo.
- 16:2x the watchman caught and filed its own measurement defect before reporting a number, which is the behavior the briefs have been asking for. The off side of every row initially had four layers on: `--no-default-features` with `--bin` but no `-p` at a workspace root selects every default member, and `boop` and `soopy` depend on `hafley-observe` with its defaults, so feature unification turned them back on for the package under test. It proved this from the rustc `--extern` lines rather than from reasoning, fixed it with `-p hafley-observe`, and wrote the row.
- 16:2x refit is at seventy-seven minutes with nothing committed, after three explicit commit orders. It has real numbers, reported over the bus: the old dylib is sha256 `606e1a3e`, 2,839,072 bytes, built from local main `39c6ac2` eighteen merges behind, and the new one from `origin/main` `68a88321f4` is sha256 `f0b808ca`, 3,112,304 bytes. Its before-set says the sqlite engine costs six to twenty-three times the Rust engine on dl8's own workload: battery 66.2/65.5/61.5 seconds against 421.7/444.5/401.7, the 1.8 MB comptime case 0.16 against 2.9 seconds, `todo.dl7` 0.15 against 3.5. Those numbers existed only in that lane's head, so they are now written into `plans/v8/2026-09-21-bench-archive.md` section 6 in the sprefa checkout, labelled a lane report rather than a receipt until the lane's own file lands. This is the first comptime timing dl8 has ever had.
- 16:3x checkpoint. Watchman's clippy leg finished clean under `-D warnings`; its tests are on their three runs. Refit is at seventy-eight minutes with an untouched tree after four commit orders, so the coordinator stopped asking and took the work directly: its `CARGO_TARGET_DIR` held 153 files and 52 MB, of which the 56 `/usr/bin/time -l` files and the dylib sha files are the durable part. They are copied into `plans/v8/receipts/2026-09-21-refit/` in the sprefa checkout and committed at `817a5c1`, with a README saying they are the lane's measurements rather than the coordinator's and that the lane's own page is the document of record once it lands. Each file carries real, user and sys seconds and maximum resident set size, so the memory axis came along with the clock; `battery-before-rust-1.time` reads 151.82 real and 1,171,079,168 bytes maximum resident. The dylib rebuild did happen: `ivm-after-sha.txt` is `f0b808ca0c...` against sqlite_ivm `origin/main` `68a88321f4`.
- 16:3x rail for every brief in this family, earned twice today. A lane's measurement outputs are committed under its worktree, never left in `CARGO_TARGET_DIR`. Four explicit orders did not move this one, and the earlier `fix-tick-transaction` incident lost a merged pull request's follow-up fix to exactly this shape. The brief should make the receipts file the first commit of the arc, before any measurement runs.
- 16:5x checkpoint. Watchman is idle at `4083ce0`, eleven commits, and the coordinator pushed the branch because the lane never had: `origin/feature/watch-the-watchman` now exists. Its self-declared done is wrong on one point and right on the rest. Right: `just watch-the-watchman` prints the twelve by three table in 592 seconds, the tsv sits beside it, clippy is clean under `-D warnings`, the owned-path diff is clean at twenty-eight files, and the off side is proven layer-free after the lane caught its own defect. Wrong: R1 claims every feature compiles alone and with `--all-features`, and `cargo test -p hafley-observe --all-features` segfaults. `tracing-tracy` 0.12 and `tracy_full` 1.13 are separate crates that each vendor `tracy-client-sys`, so the linker reports duplicate symbols across two rlibs and the bench binary dies with signal 11. Default features pass, six binaries, three runs. No PR. Grade: RED on R1, green elsewhere.
- 16:5x a rail worth carrying into every brief, found the expensive way. A receipt must not measure a quantity that the act of recording it changes. The watchman spent six commits on R8, which recorded `git diff --stat origin/main...HEAD` including the line count of the receipts page itself; writing the corrected count added a line, so the count was wrong again, 493 then 494 and onward without end. It cannot converge by construction. R8 records the output of `git diff --name-only` instead, or excludes the receipts page and says so.
- 16:5x refit committed at last, `b398eee`, one owned file, `plans/v8/2026-09-21-sqlite-ivm-refit.md`, with the before-set banked. Its after-set is landing and the first reading is unwelcome. The battery on the Rust engine went from 66.19 and 65.51 seconds warm to 119.18, 119.93 and 121.21, and peak resident went from about 1.15 GB to about 1.65 GB, a 43 percent rise. Two caveats hold that back from a verdict: the first before-run at 151.82 seconds is a cold-cache outlier, so under the strict three-run rule the spreads overlap; and the after-runs' tight consistency at 119 to 121 against a before pair at 65 to 66 says something real changed rather than that the machine was noisy. The sqlite-engine after-set has one run started and its timing file is still empty, and that is the number that answers the arc, since the sqlite path is where the twelve merged pull requests landed.
- 17:0x the arc's answer arrives, and it is the third of the three forms the brief allowed: comptime got slower. The battery on the sqlite engine, before against the old dylib, ran 421.68, 444.52 and 401.70 seconds; after against the dylib built from sqlite_ivm `origin/main` `68a88321f4` it runs 733.12 and 730.04, with the third run still going. Every after-run sits above every before-run with no overlap, so this clears the variance test, and the magnitude is three hundred seconds on a four hundred second denominator, so it clears any floor worth naming. Peak resident rose too, 1.24 to 1.31 GB before against 1.67 to 1.81 GB after. The Rust-engine battery moved the same way, 66.19 and 65.51 seconds warm against 119.18, 119.93 and 121.21, which is expected once the battery's sqlite-path tests are counted, since they load the dylib whatever `DL8_ENGINE` says. Twelve merged pull requests of engine work made dl8's own workload roughly one and three quarter times slower on both engines and cost it about forty percent more memory. The lane owes the verdict in its own words with the raw stdout; this row is the coordinator reading the lane's timing files.
- 17:0x watchman woke and took the R1 defect: its tree is dirty again on `crates/hafley-observe/Cargo.toml` and the bench source, which is where the tracy double-vendor fix belongs. Branch is pushed at `4083ce0` so nothing is at risk while it works.
- 17:1x checkpoint. Nothing exited, no PR opened by either lane, nothing to grade beyond what is already recorded, nothing dispatched; build-heavy holds at two. Watchman is on the R1 fix with `Cargo.toml`, `src/10_tracy.rs` and the bench source dirty, which is exactly where the tracy double-vendor lives; its branch is pushed so the work is safe while it edits. Refit's third sqlite after-run is still in flight, its timing file empty. Standing item unchanged from this morning's board: hafley-rs PR #86, the observe event-sums cut of 0.1.3, is still open and unpublished, and sqlite_ivm's path dependencies still point into a hafley-rs worktree rather than at a published crate.
- 17:2x `feature/watch-the-watchman` graded GREEN, PR #90, verified, awaiting merge. Twelve commits past base at `a3e5a02a`, exited rc=0. Grade run by the coordinator, not taken from the lane: `cargo test -p hafley-observe --all-features` now exits clean three runs with ten test binaries green and zero failures, where before it linked two `tracy-client-sys` rlibs and died on signal 11; `cargo clippy -p hafley-observe --all-targets --all-features -- -D warnings` reports zero diagnostics; the owned-path diff is twenty-eight files with only `Cargo.lock`, `justfile` and `docs/failure-modes.md` outside the crate, all three granted. R7 no longer records line counts, only `git diff --name-only`, which closes the self-referential receipt loop.
- 17:2x the lane's fix is better than the one the coordinator proposed. Rather than dropping `tracing-tracy`, it moved `tracy-alloc` off `tracy_full` and onto `tracy-client` 0.19, the client `tracing-tracy` already depends on, so one `tracy-client-sys` sits in the graph and both features keep working. It also found a second defect while there: the harness had never declared the global allocator, so every earlier `tracy-alloc` row measured an allocator that was not installed. Both defects are recorded as failure mode 18. The re-measured rows read tracy at plus 1.86 to 1.92 percent across the three strategies and tracy-alloc in the noise at about 3.3 million events per second, so the correction did not move the verdicts, only their honesty.
- 17:3x checkpoint. One lane live. Watchman retired clean at `a3e5a02a` after PR #90 was graded green, so build-heavy sits at one of two and a slot is open; nothing dispatched, because a spawn waits on the user's word. Refit is writing its plain-words twin, which is the last owed document.
- 17:3x the refit verdict is now complete and it holds. All three sqlite after-runs are in at 733.12, 730.04 and 735.56 seconds against a before-set of 421.68, 444.52 and 401.70. No overlap in either direction, a spread of four to five seconds on each side, and a gap of roughly three hundred seconds on a four hundred second base, so the separation is clean on the variance test and large on any floor. The Rust-engine battery matches the shape, 119.18, 119.93 and 121.21 after against 66.19 and 65.51 warm before. Peak resident rose on both. The plain reading: the sqlite_ivm work merged on 2026-09-20 and 2026-09-21 makes dl8's own battery about one and three quarter times slower. Those pull requests were each graded green against sqlite_ivm's own battery, so nothing was graded carelessly; what was missing is that nobody measured the consumer, and dl8 is the consumer. The refit lane existed to find exactly this and it did.
- 17:3x all 81 timing files, before and after, are copied into `plans/v8/receipts/2026-09-21-refit/` in the sprefa checkout and committed at `d0ca509`. Every one carries real, user and sys seconds with maximum resident set size, so the memory axis is preserved beside the clock for both engines and both dylibs.
- 17:4x checkpoint. One lane live, `feature/sqlite-ivm-refit`, nine children, still one commit at `b398eee` with its plain-words twin untracked and `Cargo.lock` dirty. Nothing exited, no new PR, nothing to grade. Build-heavy is at one of two; the open slot waits on the user's pick between lifting the labs into sqlite_ivm, fixing the dead RSS column, the queued `rail/no-scan-plans`, and a PR-review lane, so nothing is dispatched.
- 17:4x the boop replay fired a third wave, this one every `perf/toward-dd` and `perf/departure` row from today, all of them already acted on. Acked at zero days, 102 rows expired, and the box is now empty for the first time since the rebind. Three waves in one afternoon is the measure of the bug filed as `route-rebind-replays-ancient-mail`: the backlog drains in generations rather than once, so acking is not a one-shot fix and the envelope age stamp remains the real one.
- 17:5x `feature/sqlite-ivm-refit` graded GREEN, PR #811, verified, awaiting merge. Three commits, exited rc=0. Owned-path diff is exactly three files, `justfile` plus the two receipt pages, and zero paths under `src/`, `tests/`, `v5`, `v6`, `v7` or the `sqlite_ivm` symlink, so the lane changed no behavior. R1 through R9 are all present in a 433-line receipts page with the verbatim `real`, `user`, `sys` and maxrss lines for all 48 runs pasted in R4b. Clippy is red at base `e0eaec6` with eleven of thirteen findings in owned files and the lane adds none, so that leg is pre-existing and not this lane's.
- 17:5x the build-flag confound is closed, three independent ways, and the verdict survives it. The old dylib's cargo fingerprint records features `[default, extension]` where that commit's `default` was empty and no `bundled` feature existed; `rusqlite` 0.40.2's own defaults are `cache` and `ffi-sqlite-wasm-rs`, neither of which pulls a bundled library; and `otool -L` on both artifacts reads `/usr/lib/libsqlite3.dylib` 9.0.0 current 358.0.0, one exported symbol `_sqlite3_extension_init`, zero undefined. Neither dylib embeds SQLite. `origin/main` later made `bundled` a default, so the `--no-default-features` the recipe now passes is what keeps the new build equal to the old rather than what makes it differ. The comparison is like-for-like.
- 17:5x the regression is localized, and it is the one thing worth acting on. The cell is declare. dl8 declares one fresh view per program and again per comptime round; across the same 1275 SQL statements, `CREATE VIRTUAL TABLE` went from 1427.9 to 3168.7 ms and the first `insert_seeds` from 19.3 to 178.5 ms, a 9.2x jump on the seed insert. Statement count did not move, so this is per-statement cost at declaration time, which is the shape PR #809 would produce: every derived product gaining a seed table and an anchor means more tables to declare and more to touch on the first insert. That pull request was graded green on correctness against sqlite_ivm's own battery and nobody priced it against a consumer. Peak resident on the biggest comptime case went 771 to 939 MB before against 1438 to 1540 MB after, so memory moved further than the clock.
- 18:0x checkpoint. No live worker lane; both slots are free for the first time since 14:18. Mailbox empty, nothing unacked. Nothing to grade that is not already graded and recorded. Nothing dispatched: under the standing law a spawn waits on the user's word, and the open slot has four candidates on the board with no pick yet.
- 18:0x three graded-green pull requests stand verified and awaiting the user's merge call. sprefa #810, the sqlite review across every era, fifty citations across twenty-two files with three spot-checked by hand. sprefa #811, the refit, three files and no behavior change, R1 through R9 with verbatim transcripts for forty-eight runs. hafley-rs #90, the watchman, twelve by three table from one recipe, all-features green three runs after the tracy fix, clippy clean. A fourth, hafley-rs #86, predates today and is the unpublished observe cut that sqlite_ivm's path dependencies still wait on.
- 18:0x the board, dependencies all met, awaiting a pick: fix the dead `peak_rss_mib` column, 94 of 108 rows zero, now that the watchman has built the sampler; lift the v6 and v7 labs into sqlite_ivm's purview so the bench stats are referenceable everywhere; the queued `rail/no-scan-plans`, unblocked since the departure lane died; a review lane over the flash and sub-model work for leaky imports and missed traits; and the arc the refit lane just handed over, the declare cell, where `CREATE VIRTUAL TABLE` doubled and the first seed insert went 9.2x across an unchanged statement count.
- 18:0x two lanes dispatched on the user's direct order, both `flash-omp-max` on sqlite_ivm at `68a88321`, both alive at their base sha with clean trees under a minute in. Build-heavy is back at the cap of two and file ownership is disjoint: the census lane owns all of `src/**`, the rail lane is forbidden from it.
- 18:0x `feature/every-statement`, brief `plans/2026-09-21-every-statement.brief.md`. A span on every SQL statement the engine issues across all fifteen source files, carrying kind, verb, issuing `file:line`, phase, object, rows changed, and whether the statement came from the cache. Today only `1d_drain.rs` and `3_extension.rs` mention tracing; the other thirteen files run blind. Five scenarios are censused, among them one hundred inserts in one transaction against one hundred inserts in one hundred transactions, which prices the transaction boundary the `lab-xsync` work already measured. The table sorts by calls per input row, which is the indictment column, and the receipts open with a section named what this engine wastes that must name every per-row statement that could be per batch, every fresh prepare that could be cached, every DDL where nothing changed, every table declared that nothing reads, and every statement in a loop whose body does not depend on the loop. A new `tests/18_statement_census.rs` pins the counts so the next doubling fails a test rather than a benchmark.
- 18:0x `chore/timeout-rail`, brief `plans/2026-09-21-timeout-rail.brief.md`, written because a change made a consumer 1.7x slower with every gate in this repo green. Three levels, since a ceiling alone would not have caught it: a per-test budget, a per-gate ceiling, and a regression check against checked-in recorded timings, because 1.7x sits under any sane absolute ceiling. Budgets derive from three measured runs at base with the arithmetic shown, and the tolerance must be justified from the observed spread rather than chosen. R4 requires a deliberately slowed test to prove each of the three levels actually fires, with the failure output pasted, because a rail nobody has seen fail is not a rail. Build-vs-buy is enforced ahead of any code: `cargo nextest` is the obvious buy and the brief makes the lane prove or refute it against four other candidates first.
- 18:2x checkpoint. Two lanes alive, twenty minutes in. `chore/timeout-rail` committed `a5e2e7f`, the candidate table, and has `scripts/nextest.toml`, `tests/zz_rail_probe.rs` and a receipts directory untracked. `feature/every-statement` has no commit and 148 changed lines in `1d_drain.rs` plus edits to `Cargo.toml`, `1c_materialize.rs` and `lib.rs`. Nothing exited, no PR, nothing to grade, nothing dispatched; build-heavy at the cap.
- 18:2x the coordinator read the whole of `hafley-observe` on the user's order and found that most of both briefs was already built. `sqlite::instrument` at `5_sqlite.rs:132` turns on `trace_v2` for STMT and PROFILE in one call. `StatementCounters` at `:59` reads vm_step, fullscan_step, sort, autoindex, reprepare, run and mem_used per statement, and `StatementFinding` at `:35` already emits a named warning for each: table scan with no index, temporary b-tree sort, transient index built at run time, statement reprepared under a schema change. `query_plan` at `:144` wraps EXPLAIN QUERY PLAN. That set is the whole of `rail/no-scan-plans`, which was briefed this morning and is now retired as redundant. `observed_growth` and `assert_growth` at `4_counts.rs:79` assert a span's complexity class across two input sizes. `rusage::disk_io_bytes` at `8_rusage.rs:42` returns the disk bytes the toward-dd baseline reports as zero, and `rusage::layer` publishes a usage sample on every span close, which is the dead `peak_rss_mib` column.
- 18:2x the finding that matters most, and it is a process failure rather than a missing feature. `src/3_extension.rs:51-54` on origin/main already calls `hafley_observe::sqlite::instrument(db)`, gated on the trace path being set or the sqlite target being enabled at debug. The per-statement census has been available all day behind `HAFLEY_LOG=sqlite=debug`, and this log's own entry from 16:0x reads "Measure with logging off for wall; HAFLEY_LOG=sqlite=debug per-statement spans inflate and mask." The one source that says what the engine wastes was being switched off because it perturbed a stopwatch. Both lanes were corrected: the census lane builds no instrumentation and instead runs the five scenarios under that variable, adding only the phase context the callback cannot infer; the rail lane keeps a bought timeout for the catastrophic hang and makes its regression level pinned deterministic counts, since `5_sqlite.rs:90` states vm_step is the same on every machine for the same input and `4_counts.rs:78` states counts need no tolerance band.
- 18:2x the standing measurement law changes accordingly. Where a deterministic count can express a property, the rail pins the count and takes no timing at all: vm_step, fullscan_step, statement counts per phase, span complexity class. Wall-clock with three runs, a spread and a magnitude floor is the fallback for what counts cannot express, not the default. Today's arguments about cold-cache outliers and tolerance bands were avoidable.
- 18:4x checkpoint. Two lanes alive, forty minutes, no PR in the repo, nothing to grade, nothing dispatched, build-heavy at the cap.
- 18:4x `chore/timeout-rail` landed its arc at `8b9d269` and declared done before either correction reached it, so what shipped is pure wall-clock: nextest per-test budgets from a median of three, a 120 second battery ceiling, and a 1.5x or 150 millisecond recorded-wall check, with all three levels seen to fail and the gate green three times at 9.5 seconds with no added wall. Credit where it is owed: a 1.5x check would have caught the incident that started the arc, which was 1.7x. Two holes remain, both measured in this repo today rather than argued. It will flap: the refit lane's first battery run read 151.82 seconds against 66 warm on the same tree with no code change, because the first run built a binary a later test needed, and the recorded-wall check has no warmup discard. And it misses everything under its tolerance, while the cell that started the arc was `declare_view` doubling and the first seed insert going 9.2x, so a change adding forty percent per release passes forever. The lane was told to keep everything it built and add the deterministic legs beside it.
- 18:4x a fourth thing found already built. `tests/10_growth.rs:4` imports `assert_growth`, `CountRecorder` and `Growth` from hafley_observe and `:78` asserts the drain span is `Growth::Linear`, with a `Growth::Constant` assertion at `:86`; `tests/13_statements_per_drain.rs` pins statement counts. Both are green today. The deterministic rail is not missing, it covers two spans and one phase and nobody extended it as the engine grew. That is the whole gap: the engine has a per-statement tracer wired at `3_extension.rs:54`, a scan and sort and reprepare detector in `StatementFinding`, a complexity-class rail and a statement-count rail, and the declare regression still walked through every one of them.
- 18:4x `feature/every-statement` is at 412 insertions and 209 deletions across ten source files with zero commits at forty minutes, building a `census::exec` and `census::batch` wrapper that carries a `Phase` at every call site. The brief asked for ambient context for this reason: a single thread-local phase guard read by the emit callback is about twenty lines against six sites, rather than a rewrite of every statement site. Ordered to commit first and to switch only if the wrappers are not nearly done, since a rewrite now costs more than the churn it saves.
